import 'package:flutter/material.dart';
import '_wip_stub.dart';

class ExploreScreen extends StatelessWidget {
  const ExploreScreen({super.key});
  @override
  Widget build(BuildContext context) => const WipScaffold(title: 'Explore');
}
